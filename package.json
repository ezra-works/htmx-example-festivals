{
  "name": "htmx-expressjs",
  "version": "1.0.0",
  "description": "expressjs backend for htmx",
  "main": "index.js",
  "scripts": {
    "test": "echo \"Error: no test specified\" && exit 1",
    "start": "node server",
    "server": "nodemon server",
    "client": "npm run dev --prefix client",
    "dev": "concurrently \"npm run server\" \"npm run client\"",
    "build": "concurrently \"npm run start\" \"npm run build --prefix client\""
  },
  "keywords": [
    "htmx",
    "serverjs",
    "expressjs"
  ],
  "author": "Ezra Rajkumar",
  "license": "ISC",
  "dependencies": {
    "axios": "^1.6.7",
    "cors": "^2.8.5",
    "express": "^4.18.2"
  },
  "devDependencies": {
    "concurrently": "^8.2.2",
    "nodemon": "^3.0.3"
  }
}
