{% raw %}
  <div hx-ext="client-side-templates" class="m-3">
    <button
      type="button"
      class="rounded-md bg-indigo-600 px-3.5 py-2.5 text-sm font-semibold text-white shadow-sm hover:bg-indigo-500
        focus-visible:outline focus-visible:outline-2 focus-visible:outline-offset-2 focus-visible:outline-indigo-600"
      hx-get="https://api.unsplash.com/collections/n6BRQ2CgDro/photos"
      hx-swap="innerHTML"
      hx-target="#festivals"
      nunjucks-array-template="festivalList">
      Button text 3
    </button>
    <!-- tailwindui | marketing | testimonials | side-by-side -->
    <section class="bg-white py-20 sm:py-16">
      <div class="mx-auto max-w-7xl px-6 lg:px-8">
        <div id="festivals" class="mx-auto grid max-w-2xl grid-cols-1 lg:mx-0 lg:max-w-none lg:grid-cols-2"></div>
      </div>
    </section>
    <script id="festivalList" type="nunjucks">
      {% set i = -1 %}
      {% for item in data %}
        {# <p>{{ item.id }}</p> #}
        {% if item.id %}
          {% set i = i + 1 %}
          {% if i % 2 != 0 %}
            <div class="order-{{ i }} flex flex-col border-t border-gray-900/10 pt-10 sm:pt-16 lg:border-r-0 lg:border-t-0 lg:pl-8 lg:pt-0
              xl:pl-20">
            {% else %}
              <div class="order-{{ i }} flex flex-col border-t border-gray-900/10 pt-10 sm:pt-16 lg:border-r lg:border-t-0 lg:pl-8 lg:pt-0
                xl:pl-20">
              {% endif %}
              <img class="h-28 self-start" src="assets/structures.svg" alt="{{ item.alt_description }}"/>
              <figure class="m-10 flex flex-auto flex-col justify-between">
                <blockquote
                  class="text-lg leading-8 text-gray-900">
                  {# <p>“ {{ item.alt_description }}”</p> #}
                  <p>“Amet amet eget scelerisque tellus sit neque faucibus non eleifend. Integer eu praesent at a. Ornare arcu gravida
                    natoque erat et cursus tortor consequat at. Vulputate gravida sociis enim nullam ultricies habitant malesuada lorem ac.
                    Tincidunt urna dui pellentesque sagittis.”</p>
                </blockquote>
                <figcaption class="mt-10 flex items-center gap-x-6">
                  <img class="h-14 w-14 rounded-full bg-gray-50" src="{{ item.user.profile_image.large }}" alt="{{ item.user.name }}">
                    <div class="text-base">
                      <div class="font-semibold text-gray-900">{{ item.user.name }}</div>
                      <div class="mt-1 text-gray-500">{{ item.user.location }}</div>
                    </div>
                  </figcaption>
                </figure>
              </div>
            {% else %}
              This would display if the 'item' collection were empty
            {% endif %}
          {% endfor %}
        </script>
      </div>
    {% endraw %}